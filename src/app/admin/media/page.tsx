import { MediaGalleryAdmin } from "@/features/admin/media-gallery-admin";

export const metadata = { title: "Admin — Media gallery" };

export default function AdminMediaPage() {
  return <MediaGalleryAdmin />;
}
